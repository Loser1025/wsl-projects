[project]
name = "chatwork-webhook"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests==2.34.2",
    "google-auth==2.53.0",
    "google-api-python-client==2.197.0",
]

[tool.vercel]
entrypoint = "api.run:handler"
