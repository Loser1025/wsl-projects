{
  "674b1b80": {
    "base": "/tmp/mimic_subagent_React + 独自ランタイム_b51q6fa4",
    "project_dir": "/home/loser/wsl-projects",
    "task": "[あなたのロール]\nReact + 独自ランタイム混在プロジェクトの安全修復実装専門家。フェーズ0（最小リスク・確実起動）を責任実装し、変更前に必ず git で現在状態を保存する。esbuild 等の導入判断、jsx→window.CustomerCalendar バンドル、overlay 物理削除、パス整合を正確に行う。\n\n[タスク]\nプロジェクト /home/loser/wsl-projects/mission-control-v2 に対して、以下の「フェーズ0（最小リスク・確実起動）」の修復を実装・適用してください（can_write により変更がプロジェクトへ反映されます）。\n\n【最重要前処置】\n- 作業開始時に必ず `cd /home/loser/wsl-projects/mission-control-v2 && git add -A && git commit -m \"WIP: before phase0 repair\" --allow-empty` を実行し、現在の状態をコミットしてから変更を加えてください（ロールバック用）。\n\n【背景と現状（調査済み）】\n- index.html: head で `./support.js` を読み、body 先頭に `<div id=\"calendar-container\"></div>` と `<script src=\"/dist/CustomerCalendar.js\"></script>`、その後 `window.addEventListener('load', ...)` で `ReactDOM.render(React.createElement(window.CustomerCalendar), container)` する構成。FULCRUM ローディングオーバーレイは `<helmet>` タグ内の `<!-- LOADING OVERLAY -->` ブロック（`<sc-if value=\"{{ isLoading }}\">` で囲まれた fixed の白い div）。`<x-dc>` は不在。\n- support.js: `parseDcDocument` が `querySelector(\"x-dc\")` 無しで null を返し `boot()` が早期 return するため FULCRUM オーバーレイが永遠に表示される。React/ReactDOM は `loadReactUmd()` で unpkg(react@18.3.1/umd) から並列ロードし、到着後 `init()`→`__dcBoot()` する。window.React / window.ReactDOM を供給する役割を持つ。\n- CustomerCalendar.jsx: `export default CustomerCalendar`。`window.CustomerCalendar = CustomerCalendar;` はコメントアウトされている。循環参照なし。API: /api/get-availability, /api/create-booking。\n- package.json: `build: \"cp CustomerCalendar.dc.html dist/ || true\"` でバンドラなし、react 依存なし。cp はルートのファイルを探すが実体は src/components/Calendar/ にあるため失敗握りつぶし。\n- vercel.json: `outputDirectory:\".\"`, `rewrites: /(.*)→/index.html`, `headers: /CustomerCalendar.dc.html→Content-Type:text/html`（ルート想定だが実体は src/components/Calendar/ → 404）。\n- dist/CustomerCalendar.js: スタブ（「initialized」ログのみ、window.CustomerCalendar はダミー）。\n\n【フェーズ0 の実装内容（これのみ実施。フェーズ1の <x-dc> 復元はルートロジック不在のため絶対に行わない）】\n\n1. (D) ビルド整備\n   - package.json に esbuild を devDependency として追加（バージョンは環境で解決可能な最新安定版）。\n   - `build` スクリプトを esbuild で `src/components/Calendar/CustomerCalendar.jsx` をバンドルし `dist/CustomerCalendar.js` に出力するよう変更。この際 **React はバンドルせず `window.React` に依存**（external 扱い）させ、出力 JS で確実に `window.CustomerCalendar = CustomerCalendar`（実体）が登録されるようにする（jsx 側のコメントアウトを有効化するか、--banner/--footer で登録コードを付与するなど適切な手段を取ること）。\n   - 環境に esbuild が無ければ `npm install`（または npm i -D esbuild）を実行して解決すること。\n   - 既存の誤った `cp CustomerCalendar.dc.html dist/ || true` は削除または正しいパスに修正すること。\n\n2. (A)(B)(E) index.html のローディング解除・マウント確実化\n   - `<helmet>` 内の `<!-- LOADING OVERLAY -->` ブロック（FULCRUM オーバーレイ）を物理的に削除する。\n   - マウントスクリプトを「`window.React` と `window.ReactDOM` が unpkg 経由で揃うのをポーリング（上限付き）で待ち、揃ったら `ReactDOM.render(React.createElement(window.CustomerCalendar), container)` する。なお、もし overlay が残っていたらマウント完了時に削除、さらにタイムアウト(例:4秒)で強制的に overlay を削除する」安全な実装に書き換える。\n   - 壊れた `<link>` タグの断片（例: `family=Noto+Sans+JP...` のような途中で切れた記述）が body に残っている場合は削除・修正すること。\n\n3. (C) パス整合\n   - vercel.json の `headers` で `/CustomerCalendar.dc.html` を想定している行を削除（フェーズ0 では dc.html 経路は不要）。rewrites / outputDirectory は維持。\n   - dc.html の実体（src/components/Calendar/CustomerCalendar.dc.html）は現状維持（削除しない）。\n\n【検証（自身で実施）】\n- `npm install` 後 `npm run build` が成功し、生成された `dist/CustomerCalendar.js` に `window.CustomerCalendar` への実体登録（ダミー/「initialized」ログのみでないこと）が含まれることを確認すること。\n- index.html からオーバーレイブロックが消え、マウント待機ロジックになっていることを確認すること。\n\n【注意】\n- フェーズ1（<x-dc> 復元・FULCRUM 完全復元）は絶対に行わない（ルートロジックソース不在で描画エラーになるため）。\n- 変更はプロジェクトへ適用・コミットされる。失敗時は状態を元に戻してから再試行すること。\n- 最後に実施した変更の差分サマリを報告してください。",
    "label": "React + 独自ランタイム",
    "verify_cmd": "cd /home/loser/wsl-projects/mission-control-v2 && npm run build && node -e \"const fs=require('fs');const c=fs.readFileSync('dist/CustomerCalendar.js','utf8');console.log('BUILD_OK, hasRealRegistration='+(c.includes('window.CustomerCalendar') && !c.includes('initialized')));\"",
    "kind": "worker",
    "provider": "openrouter",
    "model": "tencent/hy3:free",
    "role_prompt": "あなたは以下の専門家ロールで動作するエージェントです。\n\n# ロール\nReact + 独自ランタイム混在プロジェクトの安全修復実装専門家。フェーズ0（最小リスク・確実起動）を責任実装し、変更前に必ず git で現在状態を保存する。esbuild 等の導入判断、jsx→window.CustomerCalendar バンドル、overlay 物理削除、パス整合を正確に行う。\n\n# 使えるツール\n- ファイルの読み書き・シェル実行・検索・Web調査など、委任以外の全ツールが使える\n- 変更はOverlayFS隔離された作業部屋で行われ、タスク完了後にプロジェクトへ適用される\n\n# 重要\nロールの専門性に集中し、範囲外の作業は行わない。最終回答は日本語で簡潔に結果のみ伝える。",
    "apply_changes": true,
    "started_at": "2026-07-09T11:49:34"
  }
}